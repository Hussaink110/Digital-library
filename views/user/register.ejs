<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register - Digital Library</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav>
    <div class="container" style="display:flex; justify-content:space-between; align-items:center; gap:0.5rem;">
      <h1>📚 Digital Library</h1>
      <a href="/user/login" class="btn btn-small">Login</a>
    </div>
  </nav>

  <div class="container">
    <div class="upload-form-container" style="max-width:480px;">
      <h2>Create your account</h2>
      <% if (error) { %>
        <div class="alert alert-success" style="background:#fdecea; color:#c0392b; border-color:#f5c6cb;"><%= error %></div>
      <% } %>
      <form action="/user/register" method="POST">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="form-group">
          <label for="name">Name</label>
          <input type="text" id="name" name="name" placeholder="Your name">
        </div>
        <div class="form-group">
          <label for="email">Email *</label>
          <input type="email" id="email" name="email" required placeholder="you@example.com">
        </div>
        <div class="form-group">
          <label for="phone">Phone</label>
          <input type="tel" id="phone" name="phone" placeholder="e.g. 98765 43210">
        </div>
        <div class="form-group">
          <label for="password">Password *</label>
          <input type="password" id="password" name="password" required>
        </div>
        <button class="btn btn-primary" type="submit">Sign up</button>
        <p style="margin-top:0.75rem;">Already have an account? <a href="/user/login">Login</a></p>
      </form>
    </div>
  </div>
</body>
</html>
